# macOS
.DS_Store
**/.DS_Store

# stray conversion artifacts (junk files literally named "png", ~18MB)
assets/works/*/png

# dependencies
node_modules/
.superpowers/

# personal resume (single-page html, kept out of the website)
resume/

# vercel local env + secrets (KV tokens) — never commit
.vercel/
.env*.local
.likes.json
.claude/
